Python Commands
>python -m pip install numpy
>python -m pip install --upgrade pip
>python -m pip install SpeechRecognition
>python -m pip install pocketsphinx 
google-api-python-client
tqdm
pip3 install --index-url=https://pypi.python.org/simple/ numpy
scikit-learn
sudo apt-get install python-dev

For scipy:
sudo apt-get install libblas3 liblapack3 liblapack-dev libblas-dev
sudo apt-get install gfortran
pip3 install --index-url=https://pypi.python.org/simple/ SciPy
For mglearn:
sudo apt-get install libjpeg-dev zlib1g-dev
pip install Pillow
